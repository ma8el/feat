# Generated by Feat for the application runtime of task 11111111-1111-4111-8111-111111111111 of project app.
#
# Do not edit: it is rewritten whenever the task's services are addressed. It is
# the whole application, composed of the repositories that bring it. Each entry
# carries its own project directory, so paths inside a repository's own Compose
# files resolve against that repository's checkout and nothing relative crosses
# a repository boundary.
include:
  # api
  - path:
      - "/repos/app/api/docker-compose.yml"
    project_directory: "/repos/app/api"
  # store
  - path:
      - "/repos/app/store/docker-compose.dev.yml"
    project_directory: "/repos/app/store"
